# privaelo.github.io

Personal site for Tagnon Okoumassoun — https://privaelo.github.io

Plain HTML / CSS / JS. No build step.

## Local preview

```sh
python3 -m http.server 8000
```

Then open http://localhost:8000

## Deploy

GitHub Pages deploys automatically from `main` via `.github/workflows/pages-deploy.yml`.
